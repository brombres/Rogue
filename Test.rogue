#println "Hello World"

Test()

class Test
  METHODS
    method init
      local reader = FileReader( "file.txt" )
      println reader.position
      println reader.count
      println reader.has_another
      forEach (ch in reader)
        print ch
      endForEach
      reader.close
      println
endClass

