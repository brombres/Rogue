class Test
  METHODS
    method init
      test_routine(3)
      forEach (line in LineReader("now\nis\nthe\ntime"))
        println line
      endForEach

  ROUTINES
    routine test_routine( x:Integer )
      native @|printf( "Hello World!\n" );
              |printf( "%d\n", 4 );
endClass

Test()
