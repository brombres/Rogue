class Test
  METHODS
    method init
      println String[]->String.replace(',','|')
      println String[].add("A")->String.replace(',','|')
      println String[].add("A").add("B")->String.replace(',','|')

      println "A,B,C".split(',').joined("...")
endClass

Test()
