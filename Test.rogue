local names = ["apple","banana","cherry","date"]
println names
println

names.filter( function(name:String)->Logical return not (name.count&1) )
names.filter( function(name) => not (name.count&1) )
println names
