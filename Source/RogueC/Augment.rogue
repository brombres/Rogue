class Augment
  PROPERTIES
    t            : Token
    name         : String
    base_types   = Type[]
    tokens       = Token[]
    used_modules = String[]

  METHODS
    method init( t, name, modules:String[] )
      if (modules)
        forEach (m_name in modules) used_modules.add( m_name )
      endIf
endClass
