module R2

class Template
  PROPERTIES
    name         : String
    placeholders : Token[]
    tokens       = Token[]

  METHODS
    method init( name, placeholders, tokens )

    method generate( type_args:Type[], type:Type )
      type.is_defined = true
      local class_def = Parser( tokens ).parse( Parser.ip_class )
      @trace class_def
endClass
