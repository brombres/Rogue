library RogueC

# Generated by Froley. Will not be overwritten; customize as desired.
$include "ScannerCore.rogue"

class Scanner : ScannerCore, Recyclable
  GLOBAL PROPERTIES
    uses_autoinclude   : Logical
    uses_DateTime      : Logical
    uses_File          : Logical
    uses_Introspection : Logical
    uses_Process       : Logical
    uses_Promise       : Logical
    uses_Scanner       : Logical
    uses_Set           : Logical
    uses_Table         : Logical
    uses_Variant       : Logical

  PROPERTIES
    beginning_of_line : ScannerLocation

  METHODS
    method init

    method init( file:File )
      prior.init( file )
      beginning_of_line = _scanner.location

    method init( filepath:String, content:String )
      prior.init( filepath, content )

    method init( _filepath:String, _scanner:Rogue::Scanner )
      prior.init( _filepath, _scanner )
      beginning_of_line = _scanner.location

    method on_recycle
      @_filepath = null
      @_scanner  = null

      @line   = 1
      @column = 1

      @tokens = Token[]
      @buffer.clear
      @output.clear

      @start_ip = 0
      @halt     = false

      @_position_stack.clear
      @_line_stack.clear
      @_column_stack.clear
      @_token_pos_stack.clear

      # GENERATED PROPERTIES
      @count        = 0
      @ch           = 0
      @saved_buffer = null
      @base         = 0
      @hex2         = 0
      @hex4         = 0
      @hex_digit    = 0
      @value        = 0
      @digits       = 0

    method mark_beginning_of_line
      beginning_of_line = _scanner.location

    method process_comment
      local matching_keyword : String

      contingent
        forEach (keyword in Program.todos)
          matching_keyword = keyword
          sufficient (buffer.contains(keyword) and not buffer.contains('@'+keyword))
        endForEach
        escapeContingent

      satisfied
        local comment = buffer.cloned
        local cur_loc = _scanner.location
        _scanner.location = beginning_of_line

        buffer.clear
        while (_scanner.position < cur_loc.position)
          buffer.print( _scanner.read )
        endWhile
        comment = buffer.cloned

        local matches = comment.split('\n')
        matches.discard( not $.contains(matching_keyword) )
        forEach (match in matches)
          println "[$][$:$] $"(matching_keyword,File(_filepath).filename,beginning_of_line.line,match)
        endForEach
      endContingent


    method use_DateTime
      uses_DateTime = true
      uses_autoinclude = true

    method use_File
      uses_File = true
      uses_autoinclude = true

    method use_Introspection
      uses_Introspection = true
      uses_autoinclude = true

    method use_Process
      uses_Process = true
      uses_autoinclude = true

    method use_Promise
      uses_Promise = true
      uses_autoinclude = true

    method use_Set
      uses_Set = true
      uses_autoinclude = true

    method use_Scanner
      uses_Scanner = true
      uses_autoinclude = true

    method use_Table
      uses_Table = true
      uses_autoinclude = true

    method use_Value
      use_Variant
      if (not _filepath.contains("StandardMacros.rogue"))
        Program.print_warning(
          Token(TokenType.IDENTIFIER,_filepath,0,0),
          "Type name 'Value' is deprecated and will be removed in the future. Use 'Variant' instead."
        )
      endIf

    method use_Variant
      uses_Variant = true
      uses_autoinclude = true

endClass
