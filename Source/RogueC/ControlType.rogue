module Rogue

enum ControlType( name:String )
  UNDEFINED( "undefined" )
  BLOCK( "block" )
  CATCH( "catch" )
  FOR_EACH( "forEach" )
  IF( "if" )
  LOCAL_SCOPE( "local scope" )
  LOOP( "loop" )
  RETURN( "procedure" )
  THROW( "throw" )
  TRY( "try" )
  USE( "use" )
  WHILE( "while" )

  METHODS
    method is_defined->Logical
      return (this != ControlType.UNDEFINED)
endEnum
