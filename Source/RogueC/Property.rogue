class Property
  PROPERTIES
    t             : Token
    type_context  : Type
    name          : String
    type          : Type
    attributes    : Int32
    initial_value : Cmd

  METHODS
    method init( t, type_context, name, type=null, initial_value=null )

    method clone->Property
      local result = Property( t, type_context, name )

      <<init_result>>
      result.type = type
      result.attributes = attributes
      if (initial_value) result.initial_value = initial_value.clone
      return result

    method is_incorporated->Logical
      return (attributes & Attribute.is_incorporated)?

    method mark_incorporated
      attributes |= Attribute.is_incorporated

    method set_type_context( @type_context )->Property
      return this

    method to->String
      return name
endClass

class NativeProperty( t:Token, code:String );

