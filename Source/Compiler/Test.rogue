class Alpha
  PROPERTIES
    n : Pair<<Integer,Real>>

  METHODS
    method init
      println "Multiples of 3 between 1 and 10"
      forEach (i in 1..10)
        if (i % 3 == 0) println i
      endForEach
endClass

class Pair<<$FirstType,$SecondType>>
  PROPERTIES
    first  : $FirstType
    second : $SecondType
endClass

primitive Integer
endPrimitive

primitive Logical
endPrimitive

primitive Real
endPrimitive

class String
endClass

