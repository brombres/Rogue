$include "Libraries/Standard"

class Test
  METHODS
    method init
      local points = XY[]
      points.add( XY(3,4) )
      points.add( XY(5,6) )
      points.add( XY(7,8) )
      println points
      println points.remove_first
      println points
endClass

