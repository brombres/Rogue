$include "Libraries/Standard"

class Test
  METHODS
    method init
      local reader = ParseReader(File("../Test.rogue"))
      reader.seek_to(7,14)
      print( (reader.line + "").left_justified(6) )
      forEach (ch in reader)
        print ch
        if (ch == '\n') print( (reader.line + "").left_justified(6) )
      endForEach
      println

endClass
