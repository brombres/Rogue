module ECS

class ComponentTypeManager [singleton]
  PROPERTIES
    next_type_id = ComponentSignature(1)
    count        : Int32

  METHODS
    method obtain_type_id->ComponentSignature
      local result = next_type_id
      next_type_id :<<:= 1
      ++count
      return result
endClass
