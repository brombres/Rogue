class Canvas
  PROPERTIES
    size                           : XY
    background_color = Color.BLACK : Integer

  METHODS
    method init( size )

    method clear

    method clear( background_color )
      clear

    method height->Integer [macro]
      this.size.y

    method flush

    method width->Integer [macro]
      this.size.x
endClass
