class Span( index:Int, count:Int ) [compound]
  METHODS
    method i2->Int
      return index + (count-1)

    method limit->Int
      return index + count

    method to->String
      return "@$ x$"(index,count)
endClass
