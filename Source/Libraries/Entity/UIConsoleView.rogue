module Entity

$include Entity/UIConsoleViewCanvas

class UIConsoleView : UIView
  METHODS
    method init
      prior.init( XY(Console.width, Console.height) )

    method init( size:XY )
      prior.init( size )

    method draw
      UIConsoleViewCanvas.display

    method update
      UIConsoleViewCanvas.resize( size.x, size.y )
      UIConsoleViewCanvas.fill( '.' )

endClass
