class Span( index:NewInt, count:NewInt ) [compound]
  METHODS
    method i2->NewInt
      return index + (count-1)

    method limit->NewInt
      return index + count

    method to->String
      return "@$ x$"(index,count)
endClass
