module UI

class UIScrollView : UIView
  PROPERTIES

  METHODS
    method on( e:ScrollEvent )
      forEachChild (cur,this)
        if (e.is_consumed) return
        cur.offset += e.delta
      endForEachChild (cur)

endClass
