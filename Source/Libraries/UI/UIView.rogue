module UI

class UIView : UIComponent
  METHODS
    method update_children( enclosing_bounds:Box )
      local cur = first
      while (cur)
        cur.update_layout( cur.bounds )
        cur = cur.next
      endWhile
endClass

