library UI

class Label : Component
  PROPERTIES
    text : Text

  METHODS
    method init( text:String )
      init( Text(text) )

    method init( text )
      add( text )

    method init_object
      alignment = Anchor.CENTER
endClass

